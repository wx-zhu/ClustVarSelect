% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_kmeans_bregman.R
\name{power_kmeans_bregman}
\alias{power_kmeans_bregman}
\title{Power K-means Clustering with Bregman Divergence}
\usage{
power_kmeans_bregman(
  X,
  s,
  k,
  npcs = min(50, ncol(X)),
  eta = 1.05,
  scale = TRUE,
  initial_centers = NULL,
  divergence = "euclidean",
  convergence_threshold = 5,
  seed = 1,
  max_iter = 1000,
  dim_reduction = "pca",
  bw_spectral = 1
)
}
\arguments{
\item{X}{Input data: matrix, sparse matrix (dgCMatrix), SingleCellExperiment, or Seurat object}

\item{s}{Initial power parameter for weights calculation. Must be negative (e.g., -0.5)}

\item{k}{Number of clusters}

\item{npcs}{Number of principal components for dimension reduction. Default: min(50, ncol(X))}

\item{eta}{Learning rate for power parameter updates. Default: 1.05}

\item{scale}{Logical indicating whether to scale the data. Default: TRUE}

\item{initial_centers}{Initial cluster centers matrix or NULL for random initialization}

\item{divergence}{Type of Bregman divergence: "euclidean", "kl", "itakura-saito", or "logistic"}

\item{convergence_threshold}{Number of iterations with unchanged assignments for convergence. Default: 5}

\item{seed}{Random seed for reproducibility. Default: 1}

\item{max_iter}{Maximum number of iterations. Default: 1000}

\item{dim_reduction}{Dimension reduction method: "pca", "spectral", or "none". Default: "pca"}

\item{bw_spectral}{Bandwidth parameter for spectral clustering. Default: 1}
}
\value{
List containing:
\itemize{
\item centers: Matrix of final cluster centers
\item classes: Vector of cluster assignments
\item reduced_dim: Dimension-reduced data matrix
}
}
\description{
Implements power k-means clustering algorithm with Bregman divergence for robust clustering,
supporting multiple data types and dimension reduction methods. The algorithm is particularly
effective for clustering exponential family data.
}
\details{
The algorithm implements an adaptive power k-means approach with Bregman divergence,
specifically designed for clustering exponential family data. For KL and Itakura-Saito
divergences, data is automatically shifted to ensure positivity. The power parameter s
is updated during optimization to improve clustering robustness.
}
\examples{
set.seed(123)
X <- matrix(rnorm(1000), ncol=10)
result <- power_kmeans_bregman(X, s= -0.5, k=3)

}
\references{
Vellal, A., Eldan, R., Zaslavsky, M., & Roeder, K. (2022).
Bregman Power k-Means for Clustering Exponential Family Data.
In International Conference on Machine Learning (pp. 22083-22101). PMLR.
https://proceedings.mlr.press/v162/vellal22a/vellal22a.pdf
}
